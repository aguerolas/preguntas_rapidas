# executive



The Santander applications (APMs) will be decommissioned, user access will be revoked, application functionality will be disabled, and the servers will be decommissioned once the TSA period concludes (if extended, the plan extends accordingly). This is because support with the banking entity SCB will end, and no data from that third-party entity will continue to be stored or processed.